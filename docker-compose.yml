services:
  floci:
    image: floci/floci:latest
    container_name: uptimererer-floci
    ports:
      - "4566:4566"
    volumes:
      # Floci runs Lambdas as sibling Docker containers.
      - /var/run/docker.sock:/var/run/docker.sock
    user: root
